# Example Environment Variables for Disaster Preparedness Simulator
# Copy this file to .env in each service directory and customize as needed

# =============================================================================
# BACKEND (.env in backend/)
# =============================================================================
# API_V1_STR=/api/v1
# PROJECT_NAME=Disaster Preparedness Simulator
# CORS_ORIGINS=["http://localhost:3000", "http://localhost:5173"]
# MONGODB_URL=mongodb://admin:disaster2024@localhost:27017
# MONGODB_DB_NAME=disaster_prep
# ML_ENGINE_URL=http://localhost:8001
# ENVIRONMENT=development
# DEBUG=true
# HOST=0.0.0.0
# PORT=8000

# =============================================================================
# FRONTEND (.env in frontend/)
# =============================================================================
# VITE_API_URL=http://localhost:8000
# VITE_WS_URL=ws://localhost:8000/ws
# VITE_APP_NAME=Disaster Preparedness Simulator
# VITE_APP_VERSION=1.0.0

# =============================================================================
# ML ENGINE (.env in ml-engine/)
# =============================================================================
# MODEL_PATH=./models/disaster_agent_final.zip
# MODEL_TYPE=PPO
# TOTAL_TIMESTEPS=500000
# N_ENVS=4
# EVAL_FREQ=10000
# ENVIRONMENT=development
# HOST=0.0.0.0
# PORT=8001
# TENSORBOARD_LOG=./logs
# WANDB_ENABLED=false

# =============================================================================
# DOCKER COMPOSE (Already configured in docker-compose.yml)
# =============================================================================
# The docker-compose.yml file already contains environment variables
# for running services in containers
